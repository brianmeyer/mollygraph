[pytest]
testpaths = tests service/tests
markers =
    smoke: fast, offline checks for basic service behavior.
    integration: API contract and endpoint mapping checks using TestClient.
    runtime: subprocess-based local runtime checks that exercise the real service path.
