# MollyGraph Service Configuration
# Copy this to .env and fill in your values.
# .env is loaded by config.py via python-dotenv (override=False).
# Environment variables take precedence over .env values.

# ── API Key ──────────────────────────────────────────────────────────────────
# MOLLYGRAPH_API_KEY=your-api-key-here

# ── Extraction ───────────────────────────────────────────────────────────────
# Backend: gliner2 (default) | hf_token_classification (REBEL fallback)
MOLLYGRAPH_EXTRACTOR_BACKEND=gliner2

# ── LLM Audit ────────────────────────────────────────────────────────────────
# Enable LLM-based relationship auditing (nightly/weekly)
AUDIT_LLM_ENABLED=0
# Provider order: moonshot | ollama | openrouter | none
AUDIT_PROVIDER_ORDER=none
# Model names per audit tier
AUDIT_MODEL_NIGHTLY=kimi-k2.5
AUDIT_MODEL_WEEKLY=kimi-k2.5
AUDIT_MODEL_PRETRAIN=kimi-k2.5

# ── Provider API Keys ────────────────────────────────────────────────────────
# Moonshot (Kimi) — for LLM audit
# MOONSHOT_API_KEY=sk-your-key-here

# ── Paths (optional, defaults to ~/.graph-memory) ────────────────────────────
# MOLLYGRAPH_HOME_DIR=/path/to/graph-memory
# MOLLYGRAPH_ZVEC_DIR=/path/to/zvec_collection

# ── Neo4j ────────────────────────────────────────────────────────────────────
# NEO4J_URI=bolt://localhost:7687
# NEO4J_USER=neo4j
# NEO4J_PASSWORD=mollygraph

# ── Embedding ────────────────────────────────────────────────────────────────
# Backend: sentence-transformers (default) | ollama | hash
MOLLYGRAPH_EMBEDDING_BACKEND=sentence-transformers
# Model for sentence-transformers backend
MOLLYGRAPH_EMBEDDING_MODEL=google/embeddinggemma-300m
# Model for ollama backend (if EMBEDDING_BACKEND=ollama)
# MOLLYGRAPH_OLLAMA_EMBED_MODEL=nomic-embed-text

# ── SpaCy enrichment ─────────────────────────────────────────────────────────
# Additive NER enrichment alongside GLiNER (on by default)
MOLLYGRAPH_SPACY_ENRICHMENT=1
# MOLLYGRAPH_SPACY_MODEL=en_core_web_sm
